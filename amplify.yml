version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd hospital-ui && npm ci
    build:
      commands:
        - cd hospital-ui && npm run build
  artifacts:
    baseDirectory: hospital-ui/build
    files:
      - '**/*'
  cache:
    paths:
      - hospital-ui/node_modules/**/*
